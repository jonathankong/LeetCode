using System;
using System.Collections.Generic;
using System.Text;

namespace LeetCodeSolutions
{
    class BinaryTreeTilt
    {
        //int answer = 0;
        //public int FindTilt(TreeNode root)
        //{
        //    if (root == null) return 0;
        //    else return Math.Abs(SubTreeSum(root.left) - SubTreeSum(root.right)) + answer;
        //}

        //private int SubTreeSum(TreeNode root)
        //{
        //    if (root == null) return 0;

        //    int leftTilt = SubTreeSum(root.left);
        //    int rightTilt = SubTreeSum(root.right);

        //    answer += Math.Abs(leftTilt - rightTilt);

        //    return leftTilt + rightTilt + root.val;
        //}
    }
}
